import React, { Fragment } from 'react';
import PropTypes from 'prop-types';
import { connect } from 'react-redux';
import Moment from 'react-moment';

const Stock = ({ stock }) => {
  return <Fragement>table goes here</Fragement>;
};

Stock.propTypes = {};

export default Stock;
